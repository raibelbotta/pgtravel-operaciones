{% extends 'frontend.html.twig' %}

{% form_theme form 'RentCarTypes/form.html.twig' %}

{% block content %}
    <div class="page-title">
        <div class="title_left">
            <h3>{{ 'Rent car types'|trans }} <small>{{ 'new'|trans }}</small></h3>
        </div>
    </div>
    {{ form_start(form, {'attr': {'role': 'form', 'id': 'car', 'class': 'form-horizontal form-label-left'}}) }}
        {{ form_row(form) }}
        <div class="pull-right">
            <button type="submit" class="btn btn-primary">{{ 'Save'|trans }}</button>
            <a href="{{ path('app_rentalcartypes_index') }}" class="btn btn-default">{{ 'Back to list'|trans }}</a>
        </div>
    {{ form_end(form) }}
{% endblock %}

{% block page_javascripts %}
    <script>
        $(document).ready(function() {
            App.RentCarTypes.Form.init();
        });
    </script>
{% endblock %}
