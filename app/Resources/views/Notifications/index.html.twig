{% extends 'frontend.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>{{ 'Service confirm'|trans }}</h2>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <form id="filter">
                        <div class="row">
                            <div class="col-sm-2 col-sm-offset-10 col-xs-6 col-xs-offset-6">
                                <label for="filter_state" class="control-label">{{ 'State'|trans }}</label>
                                <select id="filter_state" name="filter[state]" class="form-control input-sm filter-state">
                                    <option value=""{% if filter.state is not defined or filter.state == '' %} selected="selected"{% endif %}></option>
                                    <option value="yes"{% if filter.state is defined and filter.state == 'yes' %} selected="selected"{% endif %}>{{ 'Notified'|trans }}</option>
                                    <option value="no"{% if filter.state is defined and filter.state == 'no' %} selected="selected"{% endif %}>{{ 'No notified'|trans }}</option>
                                </select>
                            </div>
                        </div>
                    </form>
                    <div class="row">
                        <div class="col-xs-12"><br></div>
                    </div>
                    <table id="datatable-x" class="table table-striped table-bordered" width="100%">
                        <thead>
                            <tr>
                                <th>{{ 'Name'|trans }}</th>
                                <th>{{ 'Representant'|trans }}</th>
                                <th>{{ 'Client'|trans }}</th>
                                <th>{{ 'Supplier'|trans }}</th>
                                <th>{{ 'Service'|trans }}</th>
                                <th>{{ 'Start'|trans }}</th>
                                <th>{{ 'End'|trans }}</th>
                                <th>{{ 'Reference'|trans }}</th>
                                <th width="40">{{ 'Actions'|trans }}</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade in" id="modalConfirm">
        <div class="modal-dialog">
            <div class="modal-content"></div>
        </div>
    </div>
{% endblock %}

{% block page_javascripts %}
    {% javascripts filter="yui_js" output="compiled/js/notifications.js" '../web/js/notifications.js' %}
        <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}